apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "indexify.fullname" . }}-test-connection"
  labels:
    {{- include "indexify.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "indexify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
